<%- include('../partials/headerAbout') %>
<link rel="stylesheet" href="/css/style.css">
<div class="home-wrapper">

<!-- ABOUT SECTION -->
 
  <section class="aboutglass">
    <h1 class="main-title">About Our Team</h1>
    <p class="subtitle">Meet the passionate people behind this portal.</p>

    <ul class="team-list">
      <% if (team && team.length) { %>
        <% team.forEach(member => { %>
          <li>
            <strong><%= member.name %></strong>
            <span class="role">– <%= member.role %></span>
          </li>
        <% }) %>
      <% } else { %>
        <li>No team data available.</li>
      <% } %>
    </ul>
  </section>
</div>

<%- include('../partials/footer') %>

